# CI — disabled pending per-repo configuration
# See shared-context/REPO-AGENT-STANDARDS.md for setup instructions
name: CI
on:
  workflow_dispatch:  # manual trigger only
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI not yet configured for this repo"
