# Release Please — disabled pending per-repo configuration  
name: Release Please
on:
  workflow_dispatch:  # manual trigger only
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release Please not yet configured for this repo"
