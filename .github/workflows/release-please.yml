# Release Please — disabled pending per-repo configuration
# Activate by changing "workflow_dispatch" to "push: branches: [main]"
name: Release Please
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Enable by editing this workflow trigger"
